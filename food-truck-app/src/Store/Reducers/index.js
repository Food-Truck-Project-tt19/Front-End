// import actions

// initial state
const initialState = {};

// reducer function

export const reducer = (state = initialState, action) => {
    switch(action.type){
        case :
        return {}
    default:
        return state;
    }
}