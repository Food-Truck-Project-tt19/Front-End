import React, { useState, useEffect } from 'react';

// form schema
import { signInFormSchema } from './FormSchema/signInFormSchema';

// initial state
const initialState = {};

const SignIn = () => {

    // component state
    const [] = useState(initialState);

    // event handlers

    return (
        <div>
            {/* build sign in form with:
            - name
            - password
            */}
        </div>
    );

};

export default SignIn;